services:
  felica-scanner:
    build: .
    privileged: true
    network_mode: host
    environment:
      - API_URL=${API_URL:-git/scan}
      - PULSE_SERVER=unix:/run/user/1000/pulse/native
    volumes:
      - /dev/bus/usb:/dev/bus/usb
      - /sys/bus/usb:/sys/bus/usb:ro
      - /run/user/1000/pulse:/run/user/1000/pulse
      - /dev/snd:/dev/snd
    restart: unless-stopped
